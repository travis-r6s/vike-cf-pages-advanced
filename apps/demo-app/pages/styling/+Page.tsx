// TODO: Update to explain the styling this projects uses - how it uses SCSS, and imports components
// Also note this is a demo, so they would likely use styling differently.

export function Page() {
  return (
    <div className="page">
      <PageHeader title="Styling" />
      <p className="tp-body-1">Example of using Vike.</p>
    </div>
  )
}
